#summary Description of the TPJPipe.WriteData method
<wiki:comment>
$Rev$
$Date$
</wiki:comment>

= !WriteData method =

*Project:* [ConsoleAppClasses  Console Application Runner Classes].

*Unit:* _PJPipe_.

*Class:* _[TPJPipe TPJPipe]_.

{{{
function WriteData(const Buf; const BufSize: LongWord): LongWord;
}}}

== Description ==

This method writes data from a buffer to the pipe. Parameters are:
 * _Buf_: Buffer containing the data to be written. _Buf_ must have capacity of at least _!BufSize_ bytes.
 * _!BufSize_: Number of bytes to write from the buffer to the pipe.

The number of bytes actually written is returned by the method.

An _EInOutError_ exception is raised if the pipe's write handle has been closed or if the pipe can't be written to.
